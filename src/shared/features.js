// Temporary feature switches. Keep disabled features in the codebase so they
// can be restored without rebuilding their UI or database flow.
export const FEATURES = Object.freeze({
  EDITORIAL_PUBLISHING: false,
})

